**What**

**Why** (link the issue)

**How verified** (which `make` target or manual flow, on which platforms)

- [ ] `docs/recly.md` updated if a rule changed
- [ ] `make spec` passes if `spec/` changed
- [ ] No OAuth client files or keys in the diff
